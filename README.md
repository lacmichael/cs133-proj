## Basics
- All required packages are in requirements.txt, install with `pip install -r requirements.txt` (this project was created on python 3.13.7)

## How to run visualization
- To run the visualization, first make sure you have all the required packages then run `streamlit run app.py`